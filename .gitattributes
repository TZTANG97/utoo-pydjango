# Keep CRLF for Windows CI PowerShell scripts
deploy/utoo-windows/*.ps1 text eol=crlf
# Remote Linux helper scripts (LF). Do NOT add ci-entry-*.sh — runner shell must stay powershell.
deploy/utoo-windows/*.sh text eol=lf
