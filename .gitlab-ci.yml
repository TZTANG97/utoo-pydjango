# .gitlab-ci.yml — Utoo 微服务红绿双实例发版（与 EMKU 无关）
# Runner tag：utoo-windows（须为本项目单独注册，否则会 stuck）
#
# 每个环境：
# - 6 个独立手动发布：order / payment / admin_asset / admin_platform / gateway / frontend
# - 1 个一键全发：deploy_all_*（顺序跑完上面 6 步；中途失败即停）
# 前四者按各自 upstream 的空闲蓝绿实例发布并切流；gateway 独立切公网入口。
#
# Runner shell 多为 Windows PowerShell 5.1。
# 禁止在 script 里写 $$var：Runner 19 可能把 $$ 原样交给 PowerShell，解析失败却仍 Job succeeded（假绿）。
# 入口一律走 cmd -> powershell -File，用 ERRORLEVEL 保证失败能红。
# 禁止复用 emku-windows /opt/emku-* / emku_upstream_*
stages:
  - deploy

variables:
  GIT_STRATEGY: fetch
  GIT_CLEAN_FLAGS: "-ffd"
  UTOO_STATIC_WEB: "/var/www/utoo-web"
  UTOO_SHARED_CONFIG: "/opt/utoo/config"
  UTOO_UPSTREAM_CONF: "/usr/local/nginx/conf/utoo_upstream_server.conf"
  UTOO_SWITCH_SCRIPT: "/usr/local/sbin/utoo-switch-active.sh"
  # 兼容旧变量名
  UTOO_STATIC_C: "/var/www/utoo-web"
  UTOO_STATIC_ADMIN: "/var/www/utoo-web"

.utoo_windows_runner:
  tags:
    - utoo-windows

# GitLab 会展开 $CI_PROJECT_DIR；此处不再写任何 PowerShell $ 变量。
.utoo_run_deploy: &utoo_run_deploy
  - cmd.exe /c "$CI_PROJECT_DIR\\deploy\\utoo-windows\\ci-entry-deploy.cmd"

.utoo_run_frontend: &utoo_run_frontend
  - cmd.exe /c "$CI_PROJECT_DIR\\deploy\\utoo-windows\\ci-entry-frontend.cmd"

.utoo_run_all: &utoo_run_all
  - cmd.exe /c "$CI_PROJECT_DIR\\deploy\\utoo-windows\\ci-entry-all.cmd"

# --- dev：6 个独立发布 + 1 个一键全发 ---

deploy_all_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
  when: manual
  allow_failure: false
  timeout: 2h
  script: *utoo_run_all

deploy_order_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: order
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_payment_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: payment
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_admin_asset_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_asset
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_admin_platform_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_platform
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_gateway_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: gateway
    UTOO_DEPLOY_SERVICE: gateway
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_frontend_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: static
    UTOO_DEPLOY_SERVICE: frontend
  when: manual
  allow_failure: false
  script: *utoo_run_frontend

# --- prod：6 个独立发布 + 1 个一键全发 ---

deploy_all_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
  when: manual
  allow_failure: false
  timeout: 2h
  script: *utoo_run_all

deploy_order_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: order
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_payment_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: payment
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_admin_asset_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_asset
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_admin_platform_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_platform
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_gateway_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: gateway
    UTOO_DEPLOY_SERVICE: gateway
  when: manual
  allow_failure: false
  script: *utoo_run_deploy

deploy_frontend_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: static
    UTOO_DEPLOY_SERVICE: frontend
  when: manual
  allow_failure: false
  script: *utoo_run_frontend
