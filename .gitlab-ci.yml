# .gitlab-ci.yml — Utoo 微服务红绿双实例发版（与 EMKU 无关）
# Runner tag：utoo-windows（须为本项目单独注册，否则会 stuck）
#
# 每个环境有 6 个独立手动发布入口：
# order / payment / admin_asset / admin_platform / gateway / frontend
# 前四者按各自 upstream 的空闲蓝绿实例发布并切流；gateway 独立切公网入口。
#
# Runner shell 已是 powershell；用 CI_PROJECT_DIR 绝对路径调用脚本，避免相对路径找不到。
# 禁止复用 emku-windows /opt/emku-* / emku_upstream_*
stages:
  - deploy

variables:
  GIT_STRATEGY: fetch
  GIT_CLEAN_FLAGS: "-ffd"
  UTOO_STATIC_WEB: "/var/www/utoo-web"
  UTOO_SHARED_CONFIG: "/opt/utoo/config"
  UTOO_UPSTREAM_CONF: "/usr/local/nginx/conf/utoo_upstream_server.conf"
  UTOO_SWITCH_SCRIPT: "/usr/local/sbin/utoo-switch-active.sh"
  # 兼容旧变量名
  UTOO_STATIC_C: "/var/www/utoo-web"
  UTOO_STATIC_ADMIN: "/var/www/utoo-web"

.utoo_windows_runner:
  tags:
    - utoo-windows

.utoo_run_deploy: &utoo_run_deploy |
  $ErrorActionPreference = 'Stop'
  $root = if (-not [string]::IsNullOrWhiteSpace($env:CI_PROJECT_DIR)) { $env:CI_PROJECT_DIR.Trim().TrimEnd('\','/') } else { (Get-Location).Path }
  Set-Location -LiteralPath $root
  $deployScript = Join-Path $root 'deploy\utoo-windows\ci-deploy-windows.ps1'
  if (-not (Test-Path -LiteralPath $deployScript)) {
    Write-Host ("[ci] cwd={0}" -f (Get-Location).Path)
    Get-ChildItem -Name | Select-Object -First 30
    throw ("Missing deploy script: {0}" -f $deployScript)
  }
  & $deployScript

.utoo_run_frontend: &utoo_run_frontend |
  $ErrorActionPreference = 'Stop'
  $root = if (-not [string]::IsNullOrWhiteSpace($env:CI_PROJECT_DIR)) { $env:CI_PROJECT_DIR.Trim().TrimEnd('\','/') } else { (Get-Location).Path }
  Set-Location -LiteralPath $root
  $buildScript = Join-Path $root 'deploy\utoo-windows\ci-build-frontend.ps1'
  $deployScript = Join-Path $root 'deploy\utoo-windows\ci-deploy-windows.ps1'
  if (-not (Test-Path -LiteralPath $buildScript)) { throw ("Missing build script: {0}" -f $buildScript) }
  if (-not (Test-Path -LiteralPath $deployScript)) { throw ("Missing deploy script: {0}" -f $deployScript) }
  & $buildScript
  & $deployScript

# --- dev：6 个独立发布入口 ---

deploy_order_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: order
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_payment_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: payment
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_admin_asset_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_asset
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_admin_platform_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_platform
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_gateway_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: gateway
    UTOO_DEPLOY_SERVICE: gateway
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_frontend_dev:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - dev
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: dev
    UTOO_DEPLOY_PHASE: static
    UTOO_DEPLOY_SERVICE: frontend
  when: manual
  allow_failure: false
  script:
    - *utoo_run_frontend

# --- prod：6 个独立发布入口 ---

deploy_order_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: order
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_payment_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: payment
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_admin_asset_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_asset
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_admin_platform_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: service
    UTOO_DEPLOY_SERVICE: admin_platform
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_gateway_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: gateway
    UTOO_DEPLOY_SERVICE: gateway
  when: manual
  allow_failure: false
  script:
    - *utoo_run_deploy

deploy_frontend_prod:
  extends: .utoo_windows_runner
  stage: deploy
  only:
    - prod
  except:
    - merge_requests
  variables:
    UTOO_DEPLOY_TARGET: prod
    UTOO_DEPLOY_PHASE: static
    UTOO_DEPLOY_SERVICE: frontend
  when: manual
  allow_failure: false
  script:
    - *utoo_run_frontend
