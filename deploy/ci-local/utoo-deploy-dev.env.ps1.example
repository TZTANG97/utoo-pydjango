# 复制为 utoo-deploy-dev.env.ps1（勿提交）后供本机/Runner 调试加载：
#   copy deploy\ci-local\utoo-deploy-dev.env.ps1.example deploy\ci-local\utoo-deploy-dev.env.ps1
#
# 正式环境优先用 GitLab → Settings → CI/CD → Variables
# 与 EMKU 变量/路径完全分开，勿混用。

$env:DEPLOY_USER = 'deploy'
$env:DEPLOY_HOST = 'your.dev.host.or.ip'
$env:SSH_PRIVATE_KEY = @'
-----BEGIN OPENSSH PRIVATE KEY-----
...paste key...
-----END OPENSSH PRIVATE KEY-----
'@

# 可选覆盖（默认见 .gitlab-ci.yml / ci-deploy-windows.ps1）
# $env:UTOO_STATIC_WEB = '/var/www/utoo-web'
# $env:UTOO_SHARED_CONFIG = '/opt/utoo/config'
# $env:UTOO_UPSTREAM_CONF = '/usr/local/nginx/conf/utoo_upstream_server.conf'
# $env:UTOO_SWITCH_SCRIPT = '/usr/local/sbin/utoo-switch-active.sh'
# 红绿：脚本自动打空闲槽并 restart qd-*-{blue|green}，gateway 阶段切流
