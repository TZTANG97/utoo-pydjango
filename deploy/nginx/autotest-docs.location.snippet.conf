# 片段：挂载 SolarAutoTest 台架操作手册（内网静态页）
# 可 include 进 uat.utoodev.laide.tech 的 server 块（主站）。
# 静态文件来自 utoo-web-front/public/autotest-docs → 发布到 /var/www/utoo-web/autotest-docs/

location /autotest-docs/ {
    try_files $uri $uri/ /autotest-docs/index.html;
    add_header Cache-Control "no-cache";
}
