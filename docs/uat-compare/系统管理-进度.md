# 系统管理-进度

- 状态：旧站菜单盘点与只读深测完成；新站管理端登录、菜单对比、页面探活及安全 CRUD 已完成。
- 旧站登录：完成；验证到 `/console/index/main.htm` 并展开「系统管理」。
- 新站登录：完成；主机预填表单登录成功，进入 `/#/admin/dashboard`，侧边栏可见并展开「系统管理」。
- 菜单盘点：完成 11 个旧站侧边栏项，并完成新站对应 11 个菜单及独立 `ops-logs` 路由盘点。
- 列表/筛选盘点：完成 depts、users、roles、menus、districts、user-types、areas、companies、app-users、test-addresses、company-accounts、ops-logs 的列表、筛选（如有）、分页及操作按钮观察。
- 关键对比：新旧页面总体一一对应；新站 `ops-logs` 提供独立日志页；公司账户页明确保护默认账户删除按钮；App 用户页为详情只读。
- 表单安全检查：完成用户类型、区域、行政区划、测试地址等安全表单字段检查；未对用户、角色、菜单、部门、公司或默认账户执行危险写操作。
- CRUD 探活：已对新站 user-types/areas/districts/test-addresses 完成“创建→列表/查询确认→编辑→删除→列表确认”闭环；全部临时数据已清理。
- 操作日志：新站独立 `/#/admin/system/ops-logs` 可访问，已观察关键词/起始时间筛选、日志列表及分页；未执行写操作。
- 数据质量：新站区域列表仍可见重复“华东”，保留为待业务确认项，未删除既有记录。

## 安全结论

- 当前管理员账号保持可用，未禁用、删除或重置。
- 未剥离关键角色/菜单权限。
- 临时 CRUD 数据无残留；测试地址列表恢复原 6 条。

## 后续

1. 业务方确认“华东”重复记录是否应合并或保留为不同内部区域。
2. 后续发布回归时复核管理端登录入口仍可用，并继续对用户/角色/菜单/账户等高风险操作采用人工确认和审计。
